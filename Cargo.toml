[package]
name = "pyrucast"
version = "0.0.0"
edition = "2021"
description = "Finite-element library in Rust, inspired by cast3m, exposed to Python"
publish = false

[lib]
name = "pyrucast"
# cdylib: Python extension module; rlib: Rust API + tests + doctests.
crate-type = ["cdylib", "rlib"]

[dependencies]
pyo3 = { version = "0.23" }
serde = { version = "1", features = ["derive"] }
bincode = "1.3"

[features]
# Enabled only by maturin when building the Python module.
# Left off for `cargo test` / doctests (local libpython linkage works).
extension-module = ["pyo3/extension-module"]
